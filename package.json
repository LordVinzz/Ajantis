{
  "name": "ajantis",
  "version": "1.0.0",
  "description": "Ajantis Tauri shell and Rust TUI",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev:app": "tauri dev",
    "build:app": "tauri build",
    "build:tui": "cd src-tauri && cargo build --release --bin ajantis-tui --features tui",
    "dev:tui": "cd src-tauri && cargo run --bin ajantis-tui --features tui"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1"
  }
}
